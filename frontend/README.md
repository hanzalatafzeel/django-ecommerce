# VoltMart frontend

React 19 + Vite single-page application — the storefront and the admin dashboard.

```bash
npm install
npm run dev      # dev server on :5173, proxies /api, /ws, /static to Django on :8000
npm run build    # production build to dist/
```

See the [root README](../README.md) for the full setup.